name: "development-build-nonprod"
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
    build:
        name: "Build Spark Application"
        runs-on: ubuntu-latest
        steps:
        - name: "Checkout code"
          uses: actions/checkout@v3
        - name: "trigger build"
          run: "echo 'Building Spark Application...'"